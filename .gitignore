# Environment files
.env
.env.local
.env.*.local

# Development tools and AI assistant configurations
STATE.md
END_OF_DAY.md


# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/

# dist/ - Keep dist/ for JFrog deployment artifacts
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
#  Usually these files are written by a python script from a template
#  before PyInstaller builds the exe, so as to inject date/other infos into it.
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/
cover/

# Virtual environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# MacOS
.DS_Store

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# Logs
*.log
!data/samples/*.log
logs/
.tmp/

# Temporary files
*.tmp
*.temp
temp/
tmp/

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Pyre type checker
.pyre/

# pytype static type analyzer
.pytype/

# Cython debug symbols
cython_debug/

# Jupyter Notebook
.ipynb_checkpoints

# IPython
profile_default/
ipython_config.py

# Environments
.env.local
.env.development
.env.test
.env.production

# Spyder project settings
.spyderproject
.spyproject

# Rope project settings
.ropeproject

# mkdocs documentation
/site

# Node.js (for semantic-release)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json
yarn.lock

# Security and secrets
.secrets
*.key
*.pem
.env.*
!.env.example

# Build artifacts
*.whl
*.tar.gz

# Local development
local_test.py
sandbox/
experimental/

# Documentation builds
docs/_build/
docs/build/

# Test artifacts
# Test files in root only (not in scripts/)
/test_*.py
.coverage*
coverage.xml
htmlcov/

# Temporary files
.tmp/
*.tmp

# Bandit security scan results
bandit-report.json

# Safety dependency check results
pip-audit-report.json

# Package management
poetry.lock
Pipfile.lock

# pytest-benchmark
.benchmarks/

# Local CI artifacts  
act-*
.act/
.secrets.act

# CI virtual environment
.venv-ci/

# Local environment files (never commit -- contain secrets)
!.env.example.*

# Local working files (not committed)
#
# Authoritative protection for hyperi-ai content is .git/info/exclude
# (per-repo, never committed). The entries below ALSO appear there;
# duplicating them in .gitignore is belt-and-braces -- if a developer
# uses a tool that doesn't respect .git/info/exclude (unusual), the
# .gitignore still catches it. We do not RELY on these being committed
# in .gitignore -- attach surgically appends them if missing, leaves
# them alone if already present.
docs/superpowers/
.mcp.json
.ai-version

# hyperi-ai per-project context + tool-specific equivalents.
# all load these from project root natively). Local-only, never
# committed -- mirrored in .git/info/exclude.
TODO.md

# Git worktrees (local dev convenience)
.worktrees/

# Python build artefacts
dist/

# Python virtual envs and caches
.venv/
.ruff_cache/
pip-wheel-metadata/
